# assets/

- `fonts/`  — custom font files (add premium display/body faces here later).
- `images/` — app icon, splash, illustrations.

Icons in the UI are drawn as SVG in `components/ui/Icon.tsx` (no image assets needed).
Referenced `icon.png` / `splash.png` in `app.json` should be added before a native build.
